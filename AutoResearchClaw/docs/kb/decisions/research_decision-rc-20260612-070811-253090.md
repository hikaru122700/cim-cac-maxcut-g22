---
created: '2026-06-12T08:33:21+00:00'
evidence:
- stage-15/decision.md
- stage-15/decision_structured.json
id: research_decision-rc-20260612-070811-253090
run_id: rc-20260612-070811-253090
stage: 15-research_decision
tags:
- research_decision
- stage-15
- run-rc-20260
title: 'Stage 15: Research Decision'
---

# Stage 15: Research Decision

## Decision
REFINE

## Justification
The analysis must be REFINE because it fails the mandatory PROCEED gate on at least two of the five minimum criteria — and the rule is explicit: if ANY criterion is unmet, the decision is REFINE, regardless of how promising the direction looks.

- **Criterion 5 (analysis quality ≥ 4/10) — FAILS.** The Research Director's synthesis rates the result quality at **3/10**, explicitly below the threshold. This alone forecloses PROCEED.
- **Criterion 4 (no identical per-seed values / ablation integrity) — FAILS.** The two metric snapshots disagree qualitatively on identical keys (`differentiable_freeform_pump` = 0.127 vs 0.348), unprefixed aggregate keys are overwritten, and condition-level numbers "appear to copy the last seed, not a mean." CAC's exact `0.0` is flagged as "partly tautological." The analysis states plainly: "No single per-condition number is currently defensible." Ablation integrity is not established.
- **Criterion 3 (≥3 seeds) — marginal/at-risk.** n=3 is the bare minimum and carries an explicit `SEED_WARNING`; the analysis judges it "inadequate for inference."

PIVOT is not warranted: the research question is sound, there is directional signal ("control matters" is visible, the differentiable ≈ gradient-free convergence is an encouraging hint), and the apparent negative result on high-capacity methods is confounded by a 44 s undertraining budget — so the central hypothesis is *not yet* refuted, only unsupported. Refuting it prematurely (PIVOT) would discard a working harness and an unresolved question. REFINE is the correct middle path, and it matches the Director's own recommendation.

## Evidence
- **Quality rating: 3/10** (< 4/10 PROCEED gate). Infrastructure ~6–7/10, but evidential value ~2/10.
- **Data-integrity failure:** contradictory snapshots on the same keys (e.g., freeform 0.127 vs 0.348 — a qualitative reversal); aggregate-key collisions; per-condition values likely reflecting the last seed rather than a mean.
- **Off-thesis headline:** CAC (best gap, ~0.020 / 0.0086) changes the equations of motion, not the pump shape `p(t)` — it supports "feedback control helps," not the actual hypothesis. Its `C_best` reference is undocumented and possibly endogenous.
- **Hypothesis unsupported:** the best *in-thesis* schedule is the trivial `optuna_scalar_sigmoid_ramp` (~0.13); the novel freeform and spectral-conditioned nets do not beat it, and the spectral net shows large train→heldout degradation (0.365 → 0.617).
- **Protocol largely not executed:** synthetic data only (8 synthetic instances, not G15/G22/G35); TTS, dt-halving, shape-null, and ∫p dt-matching gates absent; `success_rate=1.0` means "did not crash," not scientific success.
- **Statistics:** n=3, no CIs, no significance tests, no multiple-comparison correction across 8 arms; `rank_reversal_indicator=0.0` is an underpowered null.

## Next Actions
**Blocking (must clear before any number is cited):**
1. Fix instrumentation — stop parsing stdout; emit a single structured `metrics.json`; remove the aggregate-key collision; report per-condition **mean ± bootstrap CI** over seeds. Resolve the two-snapshot contradiction and identify which pipeline produced each.

**Corrected re-run (the actual experiment):**
2. Run on the stated targets **G15 / G22 / G35**, not synthetic instances; define `C_best` from an external reference (published best-known or exact solver); restore **TTS (cut ≥ 13200)**.
3. **≥15–20 seeds** per condition; add omnibus test + Holm correction across arms.
4. **Compute-matched budgets** — equal function-evaluation budget per arm (or a budget-vs-quality curve) to dissolve the 44 s undertraining confound; document tuned-parameter count and search budget per arm in one table.
5. **Construct validity** — separate *solver* (CAC, alternative dynamics) from *schedule* (fixed dynamics, vary `p(t)`); relabel CAC as a **reference arm**, not the headline; report the schedule-only ranking.
6. **Leakage control** — for the conditioning claim, hold out *graphs* (leave-one-graph-out), document `S_train`/`S_test`, confirm optimizers did not select on the reported split.
7. **Execute skipped gates** — dt-halving (80% retention), shape-null band, ∫p dt matching; add feature-shuffle / feature-zero ablations for the spectral net, a freeform-capacity sweep, and SA / random-restart + exact-solver anchors.

**Decision rule for the next gate:** if high-dimensional / conditioned `p(t)` still loses to the scalar sigmoid after the corrected, compute-matched run, *then* PIVOT the framing toward "CAC-style feedback control + cheap scalar schedule tuning," with the most promising concrete extension being to **learn CAC's control gains through the differentiable rollout**. Until that run exists, neither PROCEED nor PIVOT is defensible.

**Operational note (from prior-run lessons):** the last attempt failed at the `code_generation` stage because no acpx session existed. Before re-running, create one: `acpx claude 

... (truncated, see full artifact)


{
  "decision": "refine",
  "raw_text_excerpt": "## Decision\nREFINE\n\n## Justification\nThe analysis must be REFINE because it fails the mandatory PROCEED gate on at least two of the five minimum criteria \u2014 and the rule is explicit: if ANY criterion is unmet, the decision is REFINE, regardless of how promising the direction looks.\n\n- **Criterion 5 (analysis quality \u2265 4/10) \u2014 FAILS.** The Research Director's synthesis rates the result quality at **3/10**, explicitly below the threshold. This alone forecloses PROCEED.\n- **Criterion 4 (no identical",
  "quality_warnings": [],
  "generated": "2026-06-12T08:33:20+00:00"
}