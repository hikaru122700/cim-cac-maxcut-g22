# num_rounds スイープ分析

- **入力グラフ**: input/G22.txt
- **Optuna**: 5 trial × CIM 10 seed (prange 並列)
- **固定パラ**: κ=130.0, BW=1e+09, photon_energy=1.28e-19
- **既知ベスト**: 13359

## チューニング後のベストパラ

| num_rounds | L | γ | loss_dB | dP/round | \|J\| | mean | std | max | min |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 300 | 0.0692 | 25.12 | 22.62 | 3.104e-04 | 0.0076 | 13197.50 | 30.14 | 13245 | 13135 |

## パラメータ単調性(num_rounds 増加方向)

- **L**: 単調増加
- **γ**: 単調増加
- **loss_dB**: 単調増加
- **dP/round**: 単調増加
- **|J|**: 単調増加

## 縮退を解いた派生量

`g₀(k) = 2κL√(k·dP)` で利得が決まり、閾値は `g₀_th = −ln(η)`。ramp 速度は `dg₀²/dk = 4κ²L²·dP`、閾値到達ラウンド予測は `k_th`。

| num_rounds | η | dg₀²/dk [/round] | g₀(N) | g₀_th | 過閾値マージン | k_th |
|---:|---:|---:|---:|---:|---:|---:|
| 300 | 0.0055 | 0.10057 | 5.493 | 5.208 | +0.285 | 270 |

**派生量の単調性:**

- **利得 ramp 速度 dg₀²/dk**: 単調増加 (ramp が遅くなる方向 = adiabatic 化)
- **閾値 g₀_th**: 単調増加 (損失を強めて閾値を持ち上げ = SN 比向上)
- **過閾値マージン g₀(N) − g₀_th**: [+0.285]

## 結論

ラウンド数が真に支配しているのは個別パラメータではなく、次の 2 つの設計変数:

- **ramp 速度** `dg₀²/dk ∝ L²·dP` ← L と dP が縮退した自由度
- **閾値** `g₀_th = −ln(η)` ← loss_dB が単独で決定

Optuna は num_rounds ごとに、この 2 つを別々の値に設定する 5 パラの組み合わせを発見:

- **短時間条件** (例: num_rounds=30): ramp 速度を上げ、閾値を下げ、強結合 + 強飽和で**強引に解を出す**
- **長時間条件** (例: num_rounds=10000): ramp 速度を下げ、閾値を上げ、弱結合 + 緩飽和で**ゆっくり adiabatic に到達**
- **中間 (300/1500)**: 総ラウンドが k_th より小さい場合は subthreshold で運用しきって最後だけ閾値付近を掠める戦略

> **将来のチューニング設計に向けたメモ**: L と dP は L²·dP の積で縮退しているので、`L_eff = L²·dP` の 1 変数化で探索効率が上がる可能性が高い。fANOVA 重要度で L と dP の個別寄与が小さく出るなら根拠あり。

---

*このファイルは `tune_cim_optuna_rounds_sweep.py` が `summary.json` から自動生成しています。*